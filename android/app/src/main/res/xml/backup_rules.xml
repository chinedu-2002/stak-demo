<?xml version="1.0" encoding="utf-8"?><!--
   Auto Backup (API < 31). The session (stak_session), every per-account state
   record (stak_state) and the copied avatars are phone-local by promise
   (Codex review, PR #166) - nothing of the user's leaves the device.
-->
<full-backup-content>
    <exclude domain="sharedpref" path="stak_session.xml"/>
    <exclude domain="sharedpref" path="stak_state.xml"/>
    <exclude domain="file" path="."/>
</full-backup-content>
